<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fank.f1k2.business.dao.DiscussionRoomMapper">

    <!-- 分页获取讨论房间表 -->
    <select id="queryPage" resultType="java.util.LinkedHashMap">
        select
        dr.id,
        dr.room_name,
        dr.topic,
        dr.images,
        dr.room_status,
        dr.max_member_count,
        dr.duration_minutes,
        dr.current_member_count,
        dr.auto_start,
        dr.start_time,
        dr.end_time,
        dr.created_by,
        dr.create_time,
        dr.update_time
        from discussion_room dr
        where 1 = 1
        <if test="queryFrom.roomName != null and queryFrom.roomName != ''">
            AND dr.room_name like CONCAT("%",#{queryFrom.roomName},"%")
        </if>
        <if test="queryFrom.topic != null and queryFrom.topic != ''">
            AND dr.topic like CONCAT("%",#{queryFrom.topic},"%")
        </if>
        <if test="queryFrom.roomStatus != null and queryFrom.roomStatus != ''">
            AND dr.room_status = #{queryFrom.roomStatus}
        </if>
        order by dr.create_time desc
    </select>
</mapper>
